function sum(a, b){
    //kiem tra
    if(typeof a !== "number" || typeof b != "number") return -1;
    // xu li
    const result = a + b;
    // ket qua
    return result;
}

console.log(sum(10 ,40));


const productLisst = [
    {id : 1 , name : "Duc"},
    {id : 1 , name : "Duc"}

];function showProduct(product){
    if(!Array.isArray(product) || product.length == 0) return -1; 
}
let resuilt =